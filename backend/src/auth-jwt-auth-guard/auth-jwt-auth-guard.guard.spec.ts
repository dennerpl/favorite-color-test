import { AuthJwtAuthGuardGuard } from './auth-jwt-auth-guard.guard';

describe('AuthJwtAuthGuardGuard', () => {
  it('should be defined', () => {
    expect(new AuthJwtAuthGuardGuard()).toBeDefined();
  });
});
